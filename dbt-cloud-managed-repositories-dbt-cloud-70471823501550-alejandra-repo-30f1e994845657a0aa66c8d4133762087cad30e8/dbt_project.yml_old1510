name: 'tpch_datavault'
version: '1.0'
config-version: 2

profile: 'tpch_datavault_profile'
model-paths: ["models"]
macro-paths: ["macros"]
models:
  tpch_datavault:
      staging:
        +materialized: view    
      raw_vault:
        +schema: raw_vault
      marts:
        +schema: marts